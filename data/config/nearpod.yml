%YAML 1.2
---
# Illuminate grade export
expected_extensions:
  - .zip
  - .zip/csv
  - .csv
contains_multiple: False
needs_matching: True # Do we need to match against something?
match_with: name_pattern
rows:
  header: 1
columns:
  name: 1
  name_pattern:
  ID:
  username:
  points_single_assignment: 5
  points_single_total:
